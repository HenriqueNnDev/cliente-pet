spring:
  application:
    name: cliente-pet
  datasource:
    url: jdbc:h2:mem:demodb
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console

server:
  servlet:
    context-path: /cliente-pet/api